import './css/Header.scss';

const Header = (props) => {
	return <header className="header">
		<div className="container">
			<div className="title">
				<span>Theme Showcase<span>
				<div className="subtitle">Argon 用户墙</div>
			</span></span></div>
		</div>
	</header>;
};

export default Header;